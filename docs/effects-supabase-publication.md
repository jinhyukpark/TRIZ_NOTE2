# Effects Supabase 게시 — 2026-09-24

## 2026-09-25 유료 구독자 전용 전환 완료 — 서버

사용자 승인: 기존 공개 이미지 포함 비공개 전환, 접근 대상은 유료 구독자만.

- `effect-content.public=false`. 기존 102개 객체는 비공개로 보존하고, 새 `private-v1/` 해시 경로에 전체 246개 이미지를 게시했다. 이전 공개 URL 샘플과 새 공개 URL 모두 HTTP 400으로 차단됨을 확인했다.
- `effect_content`: 31개 / 155단계 게시 및 read-back 일치. 이미지 246개 / 448,156,828바이트 전체 비공개 다운로드 SHA-256 일치.
- anon/authenticated 직접 테이블 접근 철회, RLS 유지. Storage 클라이언트 정책 없음. 서버 관리자만 직접 읽고 쓴다.
- `effects-content` Edge Function: `auth.getUser`로 JWT 검증 → 해당 사용자의 서버 `premium` entitlement(active, verified_at, 미래 expires_at) 확인 → DB에 있는 이미지 경로만 서명. 클라이언트의 ID·경로·user_metadata를 신뢰하지 않는다.
- 서명 URL 최대 300초, 구독 잔여 시간 이하. 응답 `Cache-Control: no-store`. 앱은 만료·백그라운드·메뉴 이탈 시 화면과 URL 참조를 잠그고, 재개 시 서버에서 다시 확인한다.
- 실서버: 미인증/익명 토큰/위조 토큰 함수 호출 401, 익명 테이블 조회 401, 관리자 발급 테스트 서명 이미지 다운로드 200.
- Metro는 Effects PNG를 빈 모듈로 치환한다. 원본 파일은 제작·게시용으로 저장소에 보존하지만, 새 앱의 번들에 넣지 않는다. 구독 확인 실패 시 이미지 원본으로 돌아가는 우회 경로를 없앤다.
- 검증: 타입 검사 및 70개 테스트 통과, iOS export 통과, Android clean release 빌드 통과. Android 소스맵의 Effects bitmap 모듈 0개, generated drawable 0개 및 APK 리소스 테이블의 Effects drawable 0개 확인. APK 약 901MiB → 253MiB. 연결된 Samsung 폰에 데이터 유지 설치(`install -r`) 성공, 앱 cold start 정상. 폰 잠금으로 마지막 구독 안내 화면의 직접 시각 검증은 잠금 해제 대기.
- 기존 배포 APK나 이미 다운로드·캡처된 이미지는 원격 회수할 수 없다. 이전 앱은 업그레이드가 필요하다. 서명 URL도 만료 전까지는 전달받은 사람이 접근할 수 있어 DRM을 의미하지 않는다.
- 현재 실제 유료 구독자는 0명이고 스토어 결제는 비활성이다. 유료 허용·만료·취소 경로는 서버 핸들러 단위 테스트로 검증했다. 실제 유료 결제 계정 E2E는 미실시이며, 검증을 위해 임의의 구독 권한을 부여하지 않았다.
- 보안 Advisor: effect_content의 RLS/no-policy INFO는 의도적 기본 거부이며, 유출 비밀번호 보호 비활성 WARN은 별도 Auth 설정 사항이다.

게시 명령은 `node scripts/publish-effects.mjs --publish --private`로 변경했다. 공개 게시 옵션은 없다. 접근 검증: `node scripts/verify-private-effects.mjs`.

아래는 전환 전의 작업 이력이다.

## 2026-09-25 전체 이전 준비 (서버 게시 승인 대기)

- 게시 스크립트 및 앱 로더를 31개 / 155단계 전체로 확장했다.
- 전체 이미지 246개, 448,156,828바이트. 기존 102개에 144개(목록 14, 장비 70, 설명 패널 60)가 추가된다.
- 기존 12개는 `renderer: legacy`로 원래 설명 이미지를 보존한다. 표피 깊이·수소화 반응은 `native-science`로 기존 네이티브 도식을 보존한다. 나머지 17개는 기존 서버 도형 데이터를 유지한다.
- 제목·본문·태그·좌표 및 이미지 참조는 서버에서 로드한다. 네이티브 도식의 렌더링 구현은 앱 코드에 남는다.
- 기존 설명 패널의 한국어가 이미지에 포함된 한계는 그대로다. 다른 언어에는 기존 번역 본문을 추가하며, 이미지 내부까지 완전한 다국어라고 보지 않는다.
- 모든 payload를 앱 검증기로 확인한 후에만 게시하도록 변경했다. 번들 이미지는 실패 시 대체용으로 유지한다.
- 공개 Storage 업로드가 자동 보안 검토에서 보류되어 사용자에게 지정 프로젝트·공개 버킷 승인을 요청했다. 아래는 **이전 17개 게시 시점의 결과**이며 전체 이전 완료를 의미하지 않는다.
- 현재 보안 Advisor에 유출 비밀번호 보호 비활성화 경고 1개가 있다. 콘텐츠 접근 정책과 별개이며 이번에 Auth 설정은 변경하지 않았다.

프로젝트: TrizNote2 (`sovzalkrotgvnqvpfkjd`). 이번 17개 신규 항목이 대상이며, 기존 14개는 로컬 구현을 유지한다.

## 서버 저장

- `public.effect_content`: 17개 게시 레코드, 85단계.
- JSON payload: 4개 언어 제목/요약/설명/핵심 원리, 단계, 출처 영상, 분류, 목록 명칭, 장비 역할, 연결선 좌표, 이미지 URL/크기/SHA-256, 설명 도형 및 번역 캡션.
- Storage `effect-content`: 목록 17개 + 상세 85개 = 102개 PNG, 202,169,071바이트.
- 콘텐츠 해시가 포함된 버전 경로로 게시하여 기존 이미지 덮어쓰기를 피한다.
- 공개 앱 콘텐츠이므로 게시 레코드와 이미지는 공개 읽기. DB RLS가 활성화되어 있고 anon/authenticated에는 SELECT만 허용한다. 클라이언트 업로드 정책은 만들지 않았다.
- 관리자 자격 증명은 CLI에서 게시 프로세스 메모리로만 전달한다. 앱, 파일, 로그에 저장하지 않는다.

## 앱 연결

Effects 메뉴 진입 시 서버 콘텐츠를 조회한다. 완전한 5단계 및 4개 언어, 신뢰하는 Storage URL을 검증한 레코드만 적용한다. 설명 도형도 서버 JSON을 네이티브 UI로 렌더링한다. 네트워크 실패/잘못된 레코드는 번들 콘텐츠를 유지하며, 이미지 다운로드 실패 시 번들 이미지로 대체한다.

렌더러 자체는 앱 코드에 남는다. 새로운 렌더링 종류나 새 항목 ID를 추가할 때에는 현재 앱 코드/번들도 함께 갱신해야 한다. 서버가 임의 코드를 실행시키는 방식은 사용하지 않는다.

## 운영 명령

`node scripts/publish-effects.mjs`: 게시 대상 수량 확인만 수행.

`node scripts/publish-effects.mjs --publish`: 지정된 TrizNote2 프로젝트에 버전 이미지 업로드, 102개 이미지 재다운로드 SHA-256 대조 후 DB upsert 및 read-back 대조. Supabase CLI의 관리자 로그인이 필요하다. 동일 해시 이미지가 있으면 재사용한다.

## 검증

- 이미지 102개 원본/서버 SHA-256 일치.
- 17개 DB payload read-back 일치.
- 앱의 publishable key로 공개 API 조회 HTTP 200: 17개, 85단계, 이미지 102개 확인.
- Supabase 보안 Advisor: 경고 없음.
- 타입 검사 및 55개 테스트 통과. 서버 데이터 교체, 잘못된 번역/URL/좌표 거부, 오프라인 보존 테스트 포함.
- iOS 번들 export 검증. App Store 배포는 수행하지 않음.

시뮬레이터에서 모든 서버 이미지·언어를 개별 조작한 전수 검증은 수행하지 않았다.
